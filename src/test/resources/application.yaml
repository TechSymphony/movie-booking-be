server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:db_dev
    driver-class-name: org.h2.Driver
  application:
    name: movie_booking
  jpa:
    hibernate:
      ddl-auto: create-drop

jwt:
  key:
    id: 'configured-f57f-46a2-b502-8a24c308be7f'
    public: classpath:app.pub
    private: classpath:app.key
